dist: trusty
language: c
addons:
  apt:
    packages:
      - libfftw3-dev
      - libgsl0-dev
env: CFLAGS=-std=gnu99
script:
  - dpkg --get-selections | grep fft
  - dpkg -L libfftw3-dev
  - find / 2>/dev/null | grep fftw3
  - make bin/blur
    #  - make
  - make test
