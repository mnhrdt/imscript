# build manifest for builds.sr.ht

image: archlinux
packages:
        - libpng
        - libtiff
        - libjpeg
        - libfftw3f
sources:
        - https://git.sr.ht/~coco/imscript/
tasks:
        - build: make -C imscript
        - test: make -C imscript test
