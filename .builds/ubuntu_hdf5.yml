# build manifest for builds.sr.ht

image: ubuntu/latest

packages:
        - libpng-dev
        - libtiff-dev
        - libjpeg-dev
        - libhdf5-dev
        - pkg-config

tasks:
        - test: make -C imscript test
