# build manifest for builds.sr.ht

image: openbsd/latest

packages:
        - png
        - tiff
        - jpeg
        - fftw3-float
        - hdf5
        - gmake


tasks:
        - test: CFLAGS="-DI_CAN_HAS_HDF5" LDLIBS="-lhdf5" gmake -C imscript test
