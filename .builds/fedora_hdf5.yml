# build manifest for builds.sr.ht

image: fedora/latest

packages:
        - png-devel
        - tiff-devel
        - jpeg-devel
        - hdf5-devel

environment:
        CFLAGS: -DI_CAN_HAS_HDF5
        LDLIBS: -lhdf5


tasks:
        - test: make -C imscript test
